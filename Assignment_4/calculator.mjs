//Export using EMS ECMASCRIPT javascript
function add(a,b){ return a+b};
let pow = (a,b) => a ** b;

export const sub = (a,b) => a + b;
export let mul = (a,b) => a * b;

export default add;
export let power = pow;

